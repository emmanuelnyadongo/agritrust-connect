export const MarketDataSource = () => (
  <div className="rounded border border-border bg-muted/20 p-3">
    <p className="text-xs font-medium text-foreground">Where does market data come from?</p>
    <p className="mt-1 text-xs text-muted-foreground">
      Low, median and high prices are supplied by the platform operator from public sources (e.g. AGRITEX bulletins, ZIMSTAT, auction data) and updated periodically. This is for guidance only; AgriTrust does not set or enforce prices.
    </p>
  </div>
);
